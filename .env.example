POSTGRES_USER=chitti
POSTGRES_PASSWORD=
POSTGRES_DB=chitti
POSTGRES_PORT=5432
LITELLM_MASTER_KEY=
GLM_CODING_API_KEY=
GLM_API_BASE=https://api.z.ai/api/paas/v4
GLM_MODEL=glm-5.2
DEEPSEEK_API_KEY=
REDIS_PASSWORD=
CHITTI_PROVIDER=litellm
CHITTI_USERNAME=akirah
CHITTI_PASSWORD_HASH=
CHITTI_SESSION_TTL_MINUTES=480
CHITTI_AUTH_STATE_PATH=/app/data/auth_state.json
CHITTI_TRUSTED_PROXY_IP=172.31.250.2
TELEGRAM_BOT_TOKEN=
ALLOWED_TELEGRAM_USER_IDS=
# Set DOMAIN to a hostname you control. For an IP-only deployment, use the
# corresponding sslip.io hostname in the server-side .env; do not commit it.
DOMAIN=
CHITTI_PORT=8000
LITELLM_PORT=4000
PROFILE_PATH=/app/profile/PROFILE.md
PROJECT_ROOT=/app/projects
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
DISPLAY_TIMEZONE=Asia/Dubai
